name: ci

on:
    push:
        branches: [main]
    pull_request:

jobs:
    test:
        runs-on: ubuntu-latest

        permissions:
            contents: read
            # Uncomment this if you want to use OIDC with Codecov instead of CODECOV_TOKEN
            # id-token: write

        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: Set up Go
              uses: actions/setup-go@v6
              with:
                  go-version: "1.22.x" # change to your version

            - name: Build
              run: go build ./...

            - name: Test with coverage
              run: go test -covermode=atomic -coverprofile=coverage.out ./...

            - name: Create coverage summary
              if: always()
              run: |
                  if [ -f coverage.out ]; then
                    go tool cover -func=coverage.out | tee coverage-summary.txt
                  fi

            - name: Generate JSON test report
              if: always()
              run: |
                  go test -json ./... > test-report.json || true

            - name: Add workflow summary
              if: always()
              run: |
                  echo "## Go CI summary" >> "$GITHUB_STEP_SUMMARY"
                  echo "" >> "$GITHUB_STEP_SUMMARY"

                  if [ -f coverage-summary.txt ]; then
                    echo "### Coverage" >> "$GITHUB_STEP_SUMMARY"
                    echo '```text' >> "$GITHUB_STEP_SUMMARY"
                    cat coverage-summary.txt >> "$GITHUB_STEP_SUMMARY"
                    echo '```' >> "$GITHUB_STEP_SUMMARY"
                  else
                    echo "No coverage summary produced." >> "$GITHUB_STEP_SUMMARY"
                  fi

                  if [ -f test-report.json ]; then
                    echo "" >> "$GITHUB_STEP_SUMMARY"
                    echo "JSON test report saved as workflow artifact: \`go-test-artifacts\`." >> "$GITHUB_STEP_SUMMARY"
                  fi

            - name: Upload test artifacts
              if: always()
              uses: actions/upload-artifact@v7
              with:
                  name: go-test-artifacts
                  path: |
                      coverage.out
                      coverage-summary.txt
                      test-report.json
                  if-no-files-found: ignore
                  retention-days: 7

            - name: Upload coverage to Codecov
              if: success()
              uses: codecov/codecov-action@v5
              with:
                  files: ./coverage.out
                  fail_ci_if_error: true
                  verbose: true
                  token: ${{ secrets.CODECOV_TOKEN }}
